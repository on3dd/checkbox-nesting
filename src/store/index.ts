import Vue from 'vue'
import Vuex from 'vuex'

import checkboxes from "@/store/modules/checkboxes";

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    checkboxes
  }
})
